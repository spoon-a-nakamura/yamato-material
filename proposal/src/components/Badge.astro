---
interface Props { tone?: 'solid' | 'outline' | 'mark'; }
const { tone = 'solid' } = Astro.props;
---
<span class={`badge ${tone === 'outline' ? 'badge--outline' : ''} ${tone === 'mark' ? 'badge--mark' : ''}`}><slot /></span>
